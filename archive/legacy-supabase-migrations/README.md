# Legacy Supabase Migrations (Archived)

This directory holds the **superseded** initial schema (`001_initial_schema.sql`, dated March 2026, v1.0).

The **canonical** migrations now live at [`supabase/migrations/`](../../supabase/migrations/) — applied via `pnpm db:migrate` and CI workflow `db-migrate.yml`. The current initial schema is [`20260423000000_initial_schema.sql`](../../supabase/migrations/20260423000000_initial_schema.sql).

Kept here only as a historical reference. **Do not apply** these against any environment.
